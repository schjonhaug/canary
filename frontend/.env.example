# Backend API URL (optional)
# Used by Next.js API routes to proxy requests to the backend
# Default: http://localhost:3000 (backend running on same host)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Authentication enabled (optional)
# Set to "false" to run in FOSS mode without authentication
# Default: "true" (authentication required)
NEXT_PUBLIC_AUTH_ENABLED=true