[package]
name = "txray"
version = "0.1.0"
edition = "2024"

[dependencies]
bdk_electrum = "0.23"
bdk_wallet = "2"
bdk_file_store = "0.17"
sqlx = { version = "0.8", features = ["runtime-tokio-rustls", "postgres", "uuid", "chrono"] }
tokio = { version = "1", features = ["full"] }
