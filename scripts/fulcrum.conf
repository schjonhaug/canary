# Fulcrum Configuration for Regtest

# Bitcoin Core connection
bitcoind = bitcoind:8332
rpcuser = bitcoin
rpcpassword = bitcoin

# Server settings
tcp = 0.0.0.0:50001
datadir = /data

# Performance settings optimized for regtest
worker_threads = 1
db_mem = 1024

# Logging
debug = 1